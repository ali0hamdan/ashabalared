# Copy to .env.production for local production builds, or set VITE_API_URL in CI (Railway, etc.).
# Must be HTTPS and must not be localhost — matches vite.config.ts production guard.
VITE_API_URL=https://api.nezhin.cc
